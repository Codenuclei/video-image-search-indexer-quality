# Same-origin proxy (app/api/proxy/[...path]/route.ts → FastAPI).
# Local: → http://127.0.0.1:8000 (API_PROXY_TARGET default)
# Railway: → https://dfi-backend-production.up.railway.app (buildArg)
# Avoids CORS; durable for long extract/generate (unlike Next rewrites).
NEXT_PUBLIC_API_URL=/api/proxy
# API_PROXY_TARGET=http://127.0.0.1:8000

# /test pages: 1 (default) = real backend via NEXT_PUBLIC_API_URL; 0 = /api/test mocks
NEXT_PUBLIC_TEST_USE_REAL_API=1

# Claude for /test topics+hooks lives in backend/.env (ANTHROPIC_API_KEY or CLAUDE_API_KEY).
# The frontend never needs the key — /test → /api/proxy → FastAPI pipeline/themes + pipeline/extract.
